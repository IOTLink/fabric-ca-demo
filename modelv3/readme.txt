问题：

fabric-sdk-go 很多配置、ca 、peer 、orderer、网络的配置都写在一个文件，
并对其中封装，使用fabric-sdk-go pkg中的config.go读取配置，保存在预先的变量struct中，
demo例子中使用的pkg中读取的借口函数读取、运行、造成用户很难配置拆散、灵活的自由组合配置，编写应用程序。


解决：
除非用户自定义配置文件、定义出解析的配置对象，提供给上层使用，否则，sdk中的难配灵活使用



